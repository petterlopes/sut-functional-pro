# Keycloak Security Configuration
# This file contains security settings for Keycloak

# SSL/TLS Configuration
kc.spi.truststore.file.hostname-verification-policy=ANY
kc.spi.truststore.file.hostname-verification-policy=ANY

# Logging Configuration
kc.log.level=INFO
kc.log.console-output=auto
kc.log.console-format=%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{1}] %m%n
kc.log.file-output=auto
kc.log.file-format=%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{1}] %m%n
kc.log.file=/opt/keycloak/data/log/keycloak.log

# Security Headers
kc.spi.truststore.file.hostname-verification-policy=ANY

# Database Configuration
kc.db=postgres
kc.db.url=jdbc:postgresql://postgres:5432/keycloak
kc.db.username=keycloak
kc.db.password=Keycloak@DB2024!

# Hostname Configuration
kc.hostname.strict=false
kc.hostname.debug=false

# HTTP Configuration
kc.http.enabled=true
kc.proxy=edge

# Health and Metrics
kc.health.enabled=true
kc.metrics.enabled=true

# Cache Configuration
kc.cache=ispn
kc.cache.config-file=infinispan.xml

# Security Settings
kc.spi.truststore.file.hostname-verification-policy=ANY
